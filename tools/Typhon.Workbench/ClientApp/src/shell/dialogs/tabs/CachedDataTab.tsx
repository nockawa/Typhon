export default function CachedDataTab() {
 return (
 <div className="flex h-full items-center justify-center text-density-sm text-muted-foreground">
 OPFS-backed cached traces land after the bootstrap phases.
 </div>
 );
}
